const form=document.querySelector('#registerForm');const msg=document.querySelector('#msg');
form?.addEventListener('submit',async e=>{e.preventDefault();msg.textContent='Sending verification link…';const data=Object.fromEntries(new FormData(form));try{const r=await fetch(`${window.API_BASE}/api/register`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(!r.ok)throw Error(j.message||'Could not register');msg.textContent='Check your email and tap the verification link to continue.';form.reset()}catch(err){msg.textContent=err.message}});
async function loadVerified(){const token=new URLSearchParams(location.search).get('verified');if(!token)return;msg.textContent='Verifying your email…';try{const r=await fetch(`${window.API_BASE}/api/verify?token=${encodeURIComponent(token)}`);const j=await r.json();if(!r.ok)throw Error(j.message||'Verification failed');localStorage.setItem('student_token',j.token);location.href='student.html'}catch(e){msg.textContent=e.message}}loadVerified();